export interface Opportunity
{
        id:number;
        opportunity_name: string;
        hiring_manager: string;
        skill: string;
        creator: string;
        creator_email: string;
        experience: number;
        job_location: string;
        joining_date: string;
        updated_date: string;
  
}

// export interface Opportunity {
//         name: string;
//         position: number;
//         weight: number;
//         symbol: string;
//       }
      

// [(ngModel)]="service.selectedOpportunity.opportunity_name"
// [(ngModel)]="service.selectedOpportunity.hiring_manager"
// [(ngModel)]="service.selectedOpportunity.skill"
// [(ngModel)]="service.selectedOpportunity.creator"
// [(ngModel)]="service.selectedOpportunity.creator_email"
// [(ngModel)]="service.selectedOpportunity.experience"
// [(ngModel)]="service.selectedOpportunity.job_location"
// [(ngModel)]="service.selectedOpportunity.joining_date"
// [(ngModel)]="service.selectedOpportunity.updated_date"